/**
 * Example Unit Test — @stackra/ts-support
 *
 * This is a placeholder test to verify the testing infrastructure works.
 * Replace with real unit tests for your package's exported classes/functions.
 *
 * Unit tests should:
 * - Test a single class or function in isolation
 * - Mock all external dependencies
 * - Be instant (no async I/O, no timers)
 * - Follow the naming convention: {class-name}.test.ts
 *
 * @example
 * ```bash
 * pnpm --filter @stackra/ts-support test
 * ```
 */

import { describe, it, expect } from "vitest";

describe("@stackra/ts-support", () => {
  it("should have a working test environment", () => {
    expect(true).toBe(true);
  });
});
