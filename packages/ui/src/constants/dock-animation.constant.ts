/**
 * @fileoverview Command dock animation constants.
 *
 * Timing and easing values for dock bar expand/collapse,
 * menu transitions, and zone positioning.
 *
 * @module constants/dock-animation
 */

/** Duration in ms for dock zone position transitions. */
export const ZONE_TRANSITION_MS = 300;

/** Duration in ms for dock bar expand/collapse. */
export const EXPAND_MS = 250;

/** Stagger delay in ms between dock button animations. */
export const STAGGER_MS = 40;

/** Duration in ms for the action menu open/close. */
export const MENU_MS = 200;

/** Opacity when the dock is faded (e.g., drawer is open). */
export const FADED_OPACITY = 0.4;
